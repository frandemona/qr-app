import 'dart:async';

import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';

// ignore_for_file: non_constant_identifier_names
// ignore_for_file: camel_case_types
// ignore_for_file: prefer_single_quotes

// This file is automatically generated. DO NOT EDIT, all your changes would be lost.
class S implements WidgetsLocalizations {
  const S();

  static S current;

  static const GeneratedLocalizationsDelegate delegate =
      GeneratedLocalizationsDelegate();

  static S of(BuildContext context) => Localizations.of<S>(context, S);

  @override
  TextDirection get textDirection => TextDirection.ltr;

  String get back => "BACK";
  String get dialogCancel => "Cancel";
  String get dialogOk => "Ok";
  String get emailLabel => "Email Address";
  String get emailValidator => "Please enter a valid email address";
  String get errorMessageTitle => "Error Message";
  String get forgotPasswordText => "Click here if you forgot your password";
  String get forgotPasswordTitleText => "Forgot Password?";
  String get loginButton => "LOGIN";
  String get loginText => "Please, Sign In";
  String get passwordLabel => "Password";
  String get passwordValidator => "Password is required";
  String get recoverPasswordButton => "SEND LINK";
  String get recoverPasswordDialogMessage =>
      "The email with recovering instructions for your account was sent";
  String get recoverPasswordDialogMessageTitle => "Message";
  String get recoverPasswordInfoText =>
      "We’ll send you a link to recover your account";
  String get title => "Navidad";
}

class $en extends S {
  const $en();
}

class $es extends S {
  const $es();

  @override
  TextDirection get textDirection => TextDirection.ltr;

  @override
  String get recoverPasswordDialogMessageTitle => "Mensaje";
  @override
  String get recoverPasswordButton => "ENVIAR LINK";
  @override
  String get back => "VOLVER";
  @override
  String get dialogCancel => "Cancelar";
  @override
  String get dialogOk => "Ok";
  @override
  String get title => "Navidad";
  @override
  String get errorMessageTitle => "Mensaje de Error";
  @override
  String get forgotPasswordText => "Haga click aquí si se olvidó su contraseña";
  @override
  String get passwordLabel => "Contraseña";
  @override
  String get loginText => "Inicie Sesión";
  @override
  String get loginButton => "INICIAR SESIÓN";
  @override
  String get emailValidator => "Por favor ingrese un correo electrónico válido";
  @override
  String get recoverPasswordDialogMessage =>
      "El correo electrónico con instrucciones para recuperar tu cuenta fue enviado";
  @override
  String get recoverPasswordInfoText =>
      "Te enviaremos un link para recuperar tu cuenta";
  @override
  String get forgotPasswordTitleText => "¿Olvidó su contraseña?";
  @override
  String get passwordValidator => "La contraseña es requerida";
  @override
  String get emailLabel => "Correo Electrónico";
}

class GeneratedLocalizationsDelegate extends LocalizationsDelegate<S> {
  const GeneratedLocalizationsDelegate();

  List<Locale> get supportedLocales {
    return const <Locale>[
      Locale("en", ""),
      Locale("es", ""),
    ];
  }

  LocaleListResolutionCallback listResolution(
      {Locale fallback, bool withCountry = true}) {
    return (List<Locale> locales, Iterable<Locale> supported) {
      if (locales == null || locales.isEmpty) {
        return fallback ?? supported.first;
      } else {
        return _resolve(locales.first, fallback, supported, withCountry);
      }
    };
  }

  LocaleResolutionCallback resolution(
      {Locale fallback, bool withCountry = true}) {
    return (Locale locale, Iterable<Locale> supported) {
      return _resolve(locale, fallback, supported, withCountry);
    };
  }

  @override
  Future<S> load(Locale locale) {
    final String lang = getLang(locale);
    if (lang != null) {
      switch (lang) {
        case "en":
          S.current = const $en();
          return SynchronousFuture<S>(S.current);
        case "es":
          S.current = const $es();
          return SynchronousFuture<S>(S.current);
        default:
        // NO-OP.
      }
    }
    S.current = const S();
    return SynchronousFuture<S>(S.current);
  }

  @override
  bool isSupported(Locale locale) => _isSupported(locale, true);

  @override
  bool shouldReload(GeneratedLocalizationsDelegate old) => false;

  ///
  /// Internal method to resolve a locale from a list of locales.
  ///
  Locale _resolve(Locale locale, Locale fallback, Iterable<Locale> supported,
      bool withCountry) {
    if (locale == null || !_isSupported(locale, withCountry)) {
      return fallback ?? supported.first;
    }

    final Locale languageLocale = Locale(locale.languageCode, "");
    if (supported.contains(locale)) {
      return locale;
    } else if (supported.contains(languageLocale)) {
      return languageLocale;
    } else {
      final Locale fallbackLocale = fallback ?? supported.first;
      return fallbackLocale;
    }
  }

  ///
  /// Returns true if the specified locale is supported, false otherwise.
  ///
  bool _isSupported(Locale locale, bool withCountry) {
    if (locale != null) {
      for (Locale supportedLocale in supportedLocales) {
        // Language must always match both locales.
        if (supportedLocale.languageCode != locale.languageCode) {
          continue;
        }

        // If country code matches, return this locale.
        if (supportedLocale.countryCode == locale.countryCode) {
          return true;
        }

        // If no country requirement is requested, check if this locale has no country.
        if (true != withCountry &&
            (supportedLocale.countryCode == null ||
                supportedLocale.countryCode.isEmpty)) {
          return true;
        }
      }
    }
    return false;
  }
}

String getLang(Locale l) => l == null
    ? null
    : l.countryCode != null && l.countryCode.isEmpty
        ? l.languageCode
        : l.toString();
